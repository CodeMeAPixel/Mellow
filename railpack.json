{
  "$schema": "https://schema.railpack.com",
  "buildAptPackages": ["ca-certificates", "tzdata", "git"],
  "steps": {
    "build": {
      "commands": [
        "go build -trimpath -ldflags=-X=main.version=$(git describe --tags --always --dirty 2>/dev/null || echo dev) -o bin/mellow ./cmd/mellow"
      ]
    }
  },
  "deploy": {
    "startCommand": "./bin/mellow"
  }
}
